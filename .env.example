# This file serves as an example for the required environment variables.
# Copy this file to .env and fill in the actual values.
# DO NOT commit the .env file to version control.

# --- Application Core Settings ---
# Set to True for development mode only. Provides more detailed error pages.
# WARNING: Do not use DEBUG=True in a production environment.
DEBUG=False

# --- Database Settings ---
# The connection string for your PostgreSQL database.
# The format is: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL="postgresql://taskops_user:a_secure_password@localhost:5432/taskops_db"

# --- Security & JWT Settings ---
# A strong, randomly generated secret key is required for signing JWTs.
# You can generate one with: openssl rand -hex 32
SECRET_KEY="your_strong_random_secret_key_here"

# The algorithm used to sign the JWT. HS256 is standard.
ALGORITHM="HS256"

# The lifetime of an access token in minutes.
ACCESS_TOKEN_EXPIRE_MINUTES=30